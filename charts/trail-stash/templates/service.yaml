apiVersion: v1
kind: Service
metadata:
  name: {{ include "trail-stash.fullname" . }}
  labels:
    {{- include "trail-stash.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "trail-stash.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
